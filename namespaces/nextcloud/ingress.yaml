apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud-ingress
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`cloud.codegameeat.com`)
      services:
        - name: nextcloud
          port: 80
  tls:
    certResolver: le